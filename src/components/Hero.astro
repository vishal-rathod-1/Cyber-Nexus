---
---
<section class="hero">
    <div class="hero-content">
        <h1>Professional <span class="text-gradient">Security Tools</span></h1>
        <p class="subtitle">Master the essential tools of cybersecurity</p>
        <div class="hero-grid">
            <div class="hero-card">
                <div class="card-icon">🔍</div>
                <h3>Network Analysis</h3>
                <p>Advanced packet inspection and network monitoring</p>
            </div>
            <div class="hero-card">
                <div class="card-icon">🛡️</div>
                <h3>Web Security</h3>
                <p>Web application testing and vulnerability assessment</p>
            </div>
            <div class="hero-card">
                <div class="card-icon">🔒</div>
                <h3>System Security</h3>
                <p>System hardening and security assessment</p>
            </div>
        </div>
    </div>
</section>

<style>
    .hero {
        padding: 4rem 2rem;
        text-align: center;
    }
    .hero-content {
        max-width: 1200px;
        margin: 0 auto;
    }
    h1 {
        font-size: 3rem;
        font-weight: 700;
        margin-bottom: 1rem;
    }
    .text-gradient {
        background: var(--accent-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    .subtitle {
        font-size: 1.25rem;
        color: var(--text-secondary);
        margin-bottom: 3rem;
    }
    .hero-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
    }
    .hero-card {
        background: rgba(var(--darker-bg), 0.8);
        padding: 2rem;
        border-radius: 8px;
        border: 1px solid rgba(var(--cyber-blue), 0.2);
        transition: all 0.3s ease;
    }
    .hero-card:hover {
        transform: translateY(-5px);
        border-color: rgba(var(--cyber-blue), 0.4);
        box-shadow: 0 5px 15px rgba(var(--cyber-blue), 0.1);
    }
    .card-icon {
        font-size: 2rem;
        margin-bottom: 1rem;
    }
    h3 {
        font-size: 1.5rem;
        margin-bottom: 1rem;
        color: rgb(var(--accent-light));
    }
    p {
        color: var(--text-secondary);
        line-height: 1.6;
    }
</style>